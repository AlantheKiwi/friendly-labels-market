
import { Product } from "@/types";

// Export an empty products list
export const productList: Product[] = [];

// Also export it as products for backward compatibility
export const products = productList;

